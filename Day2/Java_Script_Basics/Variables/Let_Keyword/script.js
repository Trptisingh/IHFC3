// Declaring variables using let
let city = "Bangalore";
let temperature = 27;

// Using the variables
let weatherMessage = "The temperature in " + city + " is " + temperature + "°C.";

// Display output in console
console.log(weatherMessage);

// Updating the variable (allowed with let)
temperature = 30;
console.log("Updated temperature: " + temperature + "°C");
